============
Installation
============

At the command line::

    $ easy_install plugs-contact

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-contact
    $ pip install plugs-contact
